# Public deployment package

This package intentionally contains no D1–D10 trip content. Before deployment, fill `firebase-config.js` with your Firebase Web `apiKey` and `appId`. Do not copy `trip-content.json` into this repository.
